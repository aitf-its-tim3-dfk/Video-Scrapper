# Core
yt-dlp
lxml>=6.0.2
cssselect>=1.4.0
orjson>=3.11.7
tld>=0.13.1
w3lib>=2.4.0
typing_extensions

# Scrapling HTTP fetcher (AsyncFetcher)
curl_cffi>=0.14.0
anyio>=4.12.1

# Scrapling fetcher dependencies (required at import time for StealthyFetcher)
browserforge>=1.2.4
patchright>=1.56.0
msgspec>=0.20.0

# Scrapling browser fetchers (DynamicFetcher / StealthyFetcher) — optional runtime
# Install playwright + chromium if you want --fetcher-mode dynamic or stealth:
# pip install playwright && python -m playwright install chromium
# playwright==1.56.0

# python -m yt_dlp --cookies cookies.txt \
#   "https://www.instagram.com/karindanashta/p/DOGcqOZk7gq/" \
#   -v --skip-download